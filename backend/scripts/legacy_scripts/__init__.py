"""Legacy smoke / UI-test scripts.

These scripts were the original way features were verified before a proper
pytest suite existed. They are kept for reference while tests are migrated
into `tests/integration/`.

DO NOT add new scripts here — write a pytest test instead.

Once every script's coverage is reproduced by a real test, delete that
script. This directory should eventually disappear.
"""
